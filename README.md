<p align="center">
  <img src="doroff_logo.svg" alt="DoroLang Logo" width="160"/>
</p>

# DoroLang — образовательный язык программирования с IDE

> **DoroLang** — это современный, простой и интуитивный интерпретируемый язык программирования с собственной кроссплатформенной IDE. Проект создан для обучения программированию с нуля, экспериментов и творчества.

---

## 🚀 Ключевые возможности

### Язык DoroLang
- Простой синтаксис, похожий на Python и псевдокод
- Переменные (`kas`), строки, числа, булевы значения (`true`/`false`)
- Арифметика (`+`, `-`, `*`, `/`, `%`), сравнения (`==`, `!=`, `<`, `>`, `<=`, `>=`)
- Логика (`and`, `or`, `not`), условные конструкции `if-else`
- Комментарии (`#`)
- Автоматическое приведение типов при конкатенации
- **Новое:** функция `input()` для интерактивного ввода данных

### IDE DoroLang
- Подсветка синтаксиса, нумерация строк
- Встроенная консоль вывода и поддержки ввода
- Быстрый запуск (F5), сохранение (Ctrl+S)
- Работа с файлами `.doro`
- Кроссплатформенность (Python + Tkinter)

---

## 🛠️ Установка и запуск

### Требования
- Python 3.6+

### Запуск IDE
```zsh
python dorolang_ide.py
```

### Использование через командную строку
- **REPL:**
  ```zsh
  python main.py interactive
  ```
- **Запуск файла:**
  ```zsh
  python main.py my_program.doro
  ```

---

## 📚 Быстрый старт: синтаксис

### Переменные и вывод
```doro
kas name = input("Как тебя зовут?")
say "Привет, " + name
```

### Условия
```doro
kas age = input("Сколько тебе лет?")
if (age >= 18) {
  say "Доступ разрешен."
} else {
  say "Доступ запрещен."
}
```

### Пример программы
```doro
# Мини-опросник
kas user = input("Введите имя:")
say "Добро пожаловать, " + user
kas ready = input("Готовы начать? (да/нет):")
if (ready == "да") {
  say "Поехали!"
} else {
  say "До встречи!"
}
```

---

## 🆕 Changelog
- **23.11.2025:** Добавлена функция `input()` для интерактивного ввода
- Улучшена документация и примеры

---

## ❓ FAQ
- **Q:** Как добавить свои функции?
  **A:** Изучите файлы `parser.py` и `interpreter.py` — архитектура открыта для расширения.
- **Q:** Можно ли запускать на Windows/Mac?
  **A:** Да, требуется только Python 3.6+.

---

## 👨‍💻 Автор
- Dorofii Karnaukh

---

<p align="center"><sub>Проект для обучения и вдохновения. Добро пожаловать в мир программирования!</sub></p>